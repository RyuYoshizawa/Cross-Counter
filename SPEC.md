# Cross_Counter 仕様書

## 1. 概要

アンケート（選択肢型設問）の集計・グラフ作成を行うStreamlitアプリ。姉妹プロジェクト「After Coder」（`C:\Users\ryu\Documents\after-coding-demo\`）・「Word_Counter」（`C:\Users\ryu\Documents\Word_Counter-demo\`）とアーキテクチャ・設計思想を揃える。

**基本方針（2026-08-19改定）**: まずGoogleフォームのアンケートに特化して制作し、完成後に一般用（他ツールで作られたアンケート）に機能拡大していく。当初はRAWデータの回答パターンから設問タイプ（単一選択/複数選択/自由記述）を推定する方式で作っていたが、Googleフォーム標準の「その他（インライン自由記述）」機能（5.4.1節）によって実データだけからは正しい設問タイプ・選択肢を再構築できないケースがあることが分かり、**アンケートフォーム原稿（PDF）をLLMに読ませて設問定義表を作る方式**に転換した（5.1節）。

**さらに2026-08-22改定**: PDF＋LLM方式は、PDFのフォントレンダリング起因の文字化け（6節）が繰り返し実害を出したため、**HTML方式（Googleフォームの編集画面をブラウザで保存したものをDOM構造から機械的に抽出、LLM不使用）を第一の方式に、PDF＋LLM方式を「うまく解析できない場合の最後の手段」に**位置付け直した（5.1.2節）。ユーザーとの合意事項:「①専用の抽出システムを持つ、②不具合があれば調査し修復を試みる、③それでもダメならPDF方式」。

最大の特徴は**設問定義表・集計指定表駆動のワンクリック実行**。細かい対話的なやり取りを重ねながら進める設計ではなく、(1) フォームPDFから設問定義表を作りRAWデータと突き合わせ、(2) どの設問をどう集計・グラフ化するかを「集計指定表」としてまとめて人が確認・調整し、(3) 確定した指定表どおりに1クリックで集計表・グラフを一括生成する、という流れを基本とする。

対象とするRAWデータは、Googleフォームで回収したスプレッドシート／CSV／Excel（5.2.1節）。他形式（選択肢番号形式、列フラグ形式等）や他ツール製アンケートへの対応は7節「今後の計画」で扱う。

## 2. 全体の流れ

```
準備: サイドバーでAnthropic APIキーを入力（接続のみ・保存しない）

0. プロジェクト開始: プロジェクト名・概要を入力

1. 設問定義表の作成（第1タブ）
   アンケートフォームのHTML（推奨、Googleフォームの編集画面を「名前を付けて保存」→
   「ウェブページ、HTMLのみ」で保存したもの）をアップロード → LLMを使わずDOM構造から
   機械的に設問定義表の初稿を作成（ID・形式・設問文・選択肢を確定情報として抽出、
   短縮設問文・短縮選択肢だけ任意でHaikuモデルに作らせる）。うまく解析できない場合は
   PDF（Googleフォームの印刷/PDF保存機能で書き出したもの）をアップロード →
   LLM（Sonnet）が設問定義表の初稿を作成（ID・形式・短縮設問文・短縮選択肢を自動生成）

2. RAWデータのアップロードまたは参照
   ダウンロードしたスプレッドシート（CSV/Excel）をアップロード、
   またはGoogleスプレッドシートのURLを直接参照

3. プロジェクト作成
   1と2の整合性をチェックし、問題なければ「プロジェクト作成」→ 第2タブが作られる

4. 設問定義表の確定
   短縮設問文・短縮選択肢の内容を確認し「設問定義表の確定」→ 以降は短縮版を使用
   （未確定のままなら原文を使用）

5. 集計指定インターフェイスへの入力と確定（第2タブ「集計指定」）
   設問×設問のチェックボックス方眼にチェックを入れると、下部の集計指定表に
   クロス集計候補が自動追加される → 内容を確認し「集計計画表を確定」

6. グラフの指示
   集計指定表の行ごとにグラフ種別（横棒/縦棒/円/帯/折れ線、指定なし可）を選ぶ

7. 実行（第3タブ「集計表」）
   共通の実行ボタン「集計表とグラフを作成する」を押すと、単純集計・クロス集計とAIコメントを
   一括生成する（実行ボタンはこのタブにのみ配置——ユーザーとの合意事項、2026-08-21）。
   集計表タブには表とAIコメント（属性間の差異の指摘）を表示し、第4タブ「グラフ」には
   同じ実行結果からグラフを表示する。
```

## 3. 全体構成（ファイル構成、予定）

```
app.py            薄いオーケストレーター（page_config, session_state初期化, sidebar呼び出し, tabs振り分け）
llm_client.py     After Coder/Word_Counterから流用するプロバイダ非依存LLM呼び出しモジュール
core/             分析ロジック（Streamlit非依存、単体テスト可能）
  ingest.py         RAWファイル読込（CSV/Excel、文字コード自動判定）、Googleスプレッドシートの直接参照（未実装）
  cleaning.py       クリーニング支援（文字化けセルの検出）
  form_html.py      アンケートフォームHTML（Googleフォーム編集画面の保存）の解析。LLM不使用、
                    DOM構造（role属性・aria-label・jsname）から機械的・決定的に設問定義表の
                    初稿を作る（第一の方式）。短縮設問文・短縮選択肢の作成にのみ任意で
                    Haikuモデルを使う（propose_short_labels、設問数が多いフォームでmax_tokens
                    上限に達し全設問分が作成されない実害があったため、設問数でバッチ分割して
                    複数回呼び出す、2026-08-22）
  form_pdf.py       アンケートフォームPDFの解析（LLMで設問定義表の初稿を作成。HTML方式が
                    使えない場合の最後の手段）
  text_normalize.py PDFフォント由来の文字化け（康熙部首ブロック・字体バリアント等）を補正する
                    正規化ヘルパー。form_pdf.py（抽出時点）・aggregate.py（値照合の都度）・
                    question_definition.py（列見出し照合）から共通で使う——HTML方式は
                    フォントレンダリングを経ないためこの問題が原理的に起こらない
  question_definition.py 設問定義表のデータモデル（ID・形式・短縮設問文・短縮選択肢・matrix・n変化）、
                    RAW列との突き合わせ（match_to_raw_columns/build_consistency_issues、5.2.3節）
  aggregate.py      単純集計・2way/3wayクロス集計の計算（母数算出ロジック含む）。選択肢テキストと
                    RAWセル値の照合は必ずNFKC正規化してから行う（全角/半角の表記ゆれを吸収する
                    ため。実データで判明した不具合の修正、2026-08-22——6節参照）。
                    find_unmatched_valuesで正規化しても対応しない値を機械的に検出できる
  cross_execute.py  確定済み集計指定表・トリプルクロス指定表を実際のRAWデータに対して一括実行する
                    オーケストレーション層（設問定義エントリ→RAW列名の対応付けを経てaggregate.py
                    の計算関数を呼ぶ。表示ラベルは短縮選択肢を優先的に使う）
  commentary.py     クロス集計結果についてAIコメント（属性間の差異の指摘、1〜2文・誇張表現なし）を
                    生成する（既定モデルはHaiku、表の数だけ呼び出しが発生するためコスト重視——
                    ユーザーとの合意事項 2026-08-21）
  usage_log.py      LLM API使用量のログ記録（作業日時・トークン数・為替レート・概算コスト）。
                    プロジェクトログタブで表示する
  charts.py         グラフ生成（Plotly。現状はGT行（単純集計）の横棒のみ実装。属性×対象の
                    クロス集計のグラフ化、縦棒/円/帯/折れ線は未実装）
  excel_export.py   Excel出力（openpyxl。旧方式の単純集計向けのみ実装済み、集計指定表からの
                    出力は未接続——実装済みの一括出力はexcel_report.py側、下記参照）
  excel_report.py   集計表タブの一括Excel出力（openpyxl、実装済み・2026-08-22）。ユーザー提示の
                    見本どおり「クロス％実数別表」「旧クロス％実数別表」「GT」「クロス％＆実数表」
                    の4シート構成でブックを作る（5.5節）
  project.py        プロジェクトファイル（.json）のシリアライズ/デシリアライズ
  data_export.py    「データ抽出」タブの「設問指定RAWデータ出力」（実装済み・2026-08-23）。指定した
                    設問だけを選び、列見出し・選択肢値を短縮/原文どちらで出すか設問ごとに切り替えて
                    CSVを組み立てる
ui/               画面（core/の結果をStreamlitで表示するだけ）
  sidebar.py        ブランディング（KOTONOHA意匠）、LLM APIキー・プロジェクト名・分析データの
                    特徴、アンケートフォームHTML（推奨）・PDF（最後の手段）・RAWファイルの
                    アップロード、プロジェクトファイルのダウンロード/アップロード。APIキー未入力
                    時はいずれのアップロードもブロックする（実装済み・2026-08-23）
  tab_question_definition.py 第1タブ「設問定義・RAWデータ確認」：設問定義表（HTML/PDF取込・初稿作成・
                    編集・確定）＋RAWデータ確認（形式確認・設問定義表との整合性チェック・
                    TEST回答削除・文字化けセル候補）
  tab_crosstab_spec.py       第2タブ「集計指定」：集計指定インターフェイス・集計指定表・グラフ指定・
                    AIコメントのオンオフ指定
  tab_crosstab_result.py     第3タブ「集計表」：共通の実行ボタン「集計表とグラフを作成する」＋
                    単純集計・2way/3wayクロス集計の表とAIコメントの表示
  tab_charts.py     第4タブ「グラフ」：集計表タブと同じ実行結果からグラフを表示（実行ボタンは
                    持たない）
  tab_data_export.py         第5タブ「データ抽出」（実装済み・2026-08-23）：「設問指定RAWデータ出力」
                    ——指定設問だけのRAWデータをCSVで書き出す
  tab_project_log.py         第6タブ「プロジェクトログ」：LLM API使用量の作業ログ一覧（最上行に
                    合計・為替は平均値）
data/
  vendor/           （必要になれば追加。現時点では未定）
```

## 4. 依存ライブラリ（予定）

| ライブラリ | 用途 |
|---|---|
| streamlit | UIフレームワーク |
| pandas | 表形式データのI/O・集計 |
| openpyxl | Excel読込・出力 |
| charset-normalizer | 文字コード自動判定 |
| plotly | グラフ生成（画面表示・インタラクティブ、After Coder/Word_Counterと同じスタック） |
| kaleido | Plotly図のPNG書き出し |
| anthropic | LLM呼び出し（PDF方式の設問定義表初稿作成、HTML方式の短縮ラベル作成、AIコメント） |
| pdfplumber | アンケートフォームPDFのテキスト抽出（PDF方式） |
| beautifulsoup4 | アンケートフォームHTMLのDOM解析（HTML方式） |

## 5. 機能仕様

### 5.0 プロジェクト管理
Word_Counterの`core/project.py`と同じ設計思想を踏襲。`st.session_state['project']`が確定するまでは分析系タブを描画しない。APIキーは保存しない（接続のみ、セッション中のみ保持）。

### 5.1 設問定義表（第1タブ）

#### 5.1.1 表の構造
1設問＝ヘッダー行（ID・形式・短縮設問文・設問文）＋選択肢の数だけ続く行（短縮選択肢・選択肢）という縦持ち形式。列構成:

| 列 | 内容 |
|---|---|
| ID | 設問ごとの識別子。フォームPDFからの自動作成時に連番（Q1, Q2…）で採番する |
| n変化 | 条件分岐で母数が全体から変わる設問は、絞り込み条件を記述する（自動抽出はあくまで参考のヒントで、対象の絞り込みには使わない。5.4.3節参照） |
| 形式 | SA（単一選択）/MA（複数選択）/FA（自由記述） |
| 短縮設問文 | 一覧表・グラフの見出し用。LLMが初稿を作成、人が編集可能 |
| 短縮選択肢 | グラフの軸ラベル用。LLMが初稿を作成、人が編集可能（選択肢の行にのみ入る） |
| 設問文（変更不可） | フォームPDFから抽出した原文（PDF方式は末尾の「*」必須マークを除去済み） |
| 選択肢（変更不可） | フォームPDFから抽出した原文（選択肢の行にのみ入る） |
| matrix | 同じ選択肢セットの設問が2つ以上連続する場合、マトリクス集計候補としてセット番号（m1-1, m1-2…）を付ける |
| 必（変更不可） | 必須設問なら「※」（実装済み、2026-08-25）。HTML方式は`aria-label="必須"`要素の`aria-checked`から、PDF方式は設問文末尾の「*」マーク（Googleフォームの印刷/PDF書き出しの慣習——`core.question_definition._normalize_question_text`が照合用に読み捨てていたのと同じ印）から、どちらも決定的に読み取る（LLMには判断させない）。人が編集する項目ではないため`disabled`（読み取り専用）。手動追加した設問（タイムスタンプ等）は常に空欄 |

列の並び順は`core.question_definition.REVIEW_COLUMNS`が決める（2026-08-27、「必」列と「n変化」列を入れ替え、上表の順に変更）。各列の表示幅はピクセル指定（`ui/tab_question_definition.py`の`_REVIEW_COLUMN_CONFIG`、`st.column_config.TextColumn(width=...)`、確定済み/未確定どちらの表示にも共通で使う）: ID 60px・n変化 90px・形式 90px・短縮設問文 200px・短縮選択肢 190px・設問文 190px・選択肢 150px・matrix 80px・必 55px（2026-08-27、ユーザー提示の画像に合わせて指定）。

最下部に表の保護/保護解除の切り替えがある（5.1.3節）。

#### 5.1.2 フォームHTML/PDFからの初稿作成
GoogleフォームはWeb上で直接編集ページを取得できない（Googleアカウントログインが必要な非公開URLのため）ので、ユーザーがオフラインでエクスポートしたファイルをアップロードしてもらう。2つの方式があり、**HTML方式を第一の方式とし、うまく解析できない場合の最後の手段としてPDF方式を維持する**（2026-08-22改定、ユーザーとの合意事項:「①専用の抽出システムを持つ、②不具合があれば調査し修復を試みる、③それでもダメならPDF方式」）。両方アップロードされている場合はHTML方式を優先する。

**HTML方式（推奨、`core/form_html.py`）**: **Googleフォームの編集画面をブラウザで「名前を付けて保存」→「ウェブページ、HTMLのみ」で保存したファイル**をアップロードしてもらう。`BeautifulSoup`でDOM構造を解析し、**LLMを使わず機械的・決定的に**設問ID・形式（SA/MA/FA）・設問文・選択肢を抽出する。短縮設問文・短縮選択肢の作成にだけ、任意で低コストのHaikuモデルを使う（`propose_short_labels`。APIキー未設定でも構造自体は完成する——人が後で短縮ラベルを手入力すればよい）。

抽出に使う構造的な手がかり（Googleの内部CSSクラス名は実装の詳細でありUI更新で変わり得るため避け、role属性・aria-label・jsnameという比較的安定した信号を使う——特にaria-labelは実際のユーザー向け日本語UI文言そのものであり、Googleが気軽に変えるものではない）:
- 設問カードの境界: `jsname="Mxc6Ne"`（実データで実際の設問数と一致することを確認済み）
- 設問文: カード内の`aria-label="質問" role="textbox"`のうち最初の1件
- 選択肢: カード内の`role="list"`の直接の子要素のうち`role="radio"`（単一選択）/`role="checkbox"`（複数選択）を含むもの——**カード全体ではなくrole="list"の中だけを見ることが重要**（「質問の形式」ドロップダウン自体がradio styleの項目を大量に持つため、カード全体を見ると実際の選択肢と誤認する）
- 自由記述: `role="list"`が無く、`aria-label="説明"`（完全一致。セクションの「説明（省略可）」とは別）のrole="textbox"（回答プレビュー欄）がある
- セクション見出し: `aria-label="セクション タイトル（省略可）"`/`"説明（省略可）"`。スキップロジックの説明文言（「前問で、〇〇とお答えの方にお伺いします」等）がここに入っていることが多い。**この文言は同じセクション内の設問すべてのn_noteに使う（2026-08-26、設計変更）**——Googleフォームのページ区切り＝セクション単位で分岐先が決まるため、1セクションに複数の設問が含まれる場合はその全てに適用されるべきだが、当初は直後の1設問にしか引き継いでおらず、同じ分岐セクション内で2問目以降の設問のn_noteを見落とすケースが実データで見つかった。「セクション タイトル」マーカーに出会うたびに新セクションの開始としてリセットし（文言が無ければ空に）、直後の「説明」マーカーは文言がある場合のみ上書きする（空の「説明」マーカーが必ずタイトルの直後に続くため、単純に「マーカーの文言が無ければ毎回空に戻す」実装だとタイトル側の文言を打ち消してしまう——実データのHTML構造では分岐の説明文言はタイトル側に入っていた）。質問カードを処理してもリセットしない（同じセクション内では引き継ぎ続ける）。
- 選択肢テキストには分岐先ナビゲーション文言（「次のセクションに進む」等）が地の文として混入するため、既知パターンを除去する
- 均等目盛・グリッド形式・日付・時刻・ファイルのアップロードは非対応として取り込まず、件数を警告として返す（ユーザーとの合意事項）

実際の26問の実データで、警告0件・全問正確に抽出できることを確認済み（`testスプレッドシート - 出力.csv`という、別途Apps Script（`FormApp`公式APIを使用、`kwaka1208/gas-export-form-to-spreadsheet`）で作った参照用の正解データと完全一致）。このApps Script自体は個人制作で未検証アプリの権限確認が必要になるため、正式な取込経路としては採用しなかった（参照データ作成にのみ使用）。

**PDF方式（最後の手段、`core/form_pdf.py`）**: **Googleフォームの印刷/PDF保存機能で書き出したPDF**をアップロードしてもらう。`pdfplumber`でテキストを抽出し、LLM（Sonnet）に構造化データ（設問ID・形式・設問文・選択肢・matrix候補・短縮設問文案・短縮選択肢案）として整形させる。フォントレンダリングを経るため、6節で詳述する文字化けの実害が繰り返し見つかっている——HTML方式ならこの問題が原理的に起こらない。

**LLM出力のスキーマ逸脱への耐性（実データで判明、2026-08-22、PDF方式）**: LLMがまれに配列要素をスキーマ通りのオブジェクトではなく文字列で返すことがあり（設問1件、または選択肢1件だけがこうなるケースを実際に確認）、`build_entries`が`AttributeError: 'str' object has no attribute 'get'`でクラッシュしていた。`core/question_definition.py`の`build_entries`は辞書でない要素を黙ってスキップするよう修正し（`count_invalid_questions`で件数を取得可能）、`ui/tab_question_definition.py`は除外件数があれば「PDFから作り直す」を促す警告を表示する。1件のスキーマ逸脱で設問定義表全体の作成が失敗しないようにする、という設計。

#### 5.1.3 編集・確定
（変更不可）列以外は直接修正できる。内容に問題がなければ「設問定義表の確定」ボタンを押すと表が保護され、以降の集計・グラフ・一覧表では短縮設問文・短縮選択肢が使われる（未確定のままなら原文を使う）。保護後も「保護を解除する」で再編集できる。**画面の並び順は、表本体→切り替えボタン（「設問定義表の確定」または「保護を解除する」、状態に応じてラベルが変わる同じボタン）＋「HTMLから作り直す」（PDF方式なら「PDFから作り直す」）ボタンの横並び→（確定済みの場合のみ）確定済みメッセージ→プロジェクトファイルのダウンロードを勧める案内、の順**（2026-08-27、配置変更——確定済み/未確定のどちらの状態でも同じ順序になるよう統一した）。

#### 5.1.4 設問の手動追加
フォームPDFには出てこないがRAWデータには存在する列（Googleフォームが自動追加する「タイムスタンプ」列など）のために、設問文・形式・短縮設問文・選択肢（SA/MAのみ）を入力して1行追加できる（挿入位置は先頭/末尾を選択）。RAW列見出しと一字一句同じ設問文を入力すれば、5.2.3節の整合性チェックで自動的に対応付けられる。実装は`core/question_definition.py`の`add_manual_entry`。フォームの初期値は最頻出ケースの「タイムスタンプ」（形式FA・設問文/短縮設問文とも「タイムスタンプ」）にしてあり、そのまま送信すれば固定ID「time」で追加される（このIDはQ1/Q2/…の通し番号の振り直し対象から除外され、他の設問の追加・並び替えでQ番号がずれても変わらない）。それ以外の設問文で追加した場合は通常どおりQ番号が振られる。既にID「time」の設問がある状態で「タイムスタンプ」を再送信すると警告を出し、二重追加はしない。**このフォームは設問定義表が確定（保護）済みでも常に表示される**（実装当初は未確定時のみ表示していたが、確定して集計まで進めた後にタイムスタンプの追加漏れに気づく、という実際の使われ方に合わず、確定状態に関わらず表示するよう修正した、2026-08-22）。**画面上の配置は設問定義表そのものより下**（2026-08-27、配置変更——当初は表より上に置いていた）。

#### 5.1.5 設問定義表のアップロードによる差し替え（実装済み、2026-08-26）
短縮設問文・短縮選択肢・matrix・n変化を大量にまとめて編集したい場合向けに、表（`st.data_editor`/`st.dataframe`）標準のダウンロードアイコン（画面右上部）で出力したCSVをオフラインで編集し、「📤 設問定義表をアップロードして差し替える」（設問の手動追加フォームの直下、5.1.4節と同じく確定状態に関わらず常に表示）からアップロードして反映できる。ダウンロード機能はStreamlitの標準ツールバーに既にあるため、新規実装したのはアップロード側のみ。

アップロードされたファイル（CSV/Excel、`core/ingest.py`の`read_raw_file`で文字コード自動判定）は、必須列（`REVIEW_COLUMNS`）が揃っているかをまず確認し、次に`core.question_definition.find_review_upload_mismatches`で**構造の一致**を確認する——ID列（設問ヘッダー行の目印）の並びが現在の設問定義表と完全に一致するか、および各設問ヘッダーに続く選択肢行の件数が一致するかの2点。これは、行の追加・削除・並べ替え等でファイルの構成が変わっていた場合、`apply_review_edits`（ID一致・順序対応の選択肢行という前提で動く既存の反映ロジック、そのままこの機能でも再利用）が誤った行に値を書き込んでしまう事故を防ぐため。一致しない場合は反映せず、具体的な不一致内容（無い/余分なID、選択肢行数の差）を一覧でアラート表示する。一致すれば通常の編集確定と同じ`apply_review_edits`で反映する（ID・設問文・選択肢は元々このロジックが変更しないため、アップロード側でこれらを書き換えても無視される——画面の`st.data_editor`で`disabled`指定しているのと同じ保護が自動的にかかる）。

### 5.2 RAWデータの取込

#### 5.2.1 対象形式
Googleフォーム標準エクスポート（CSV/Excel）を対象とする。1行目が設問文（改行・注記を含む場合あり）のヘッダー、2行目以降が回答者ごとの行。単一選択はセルに1つの選択肢テキスト、複数選択はセル内に`, `区切りで列挙、条件分岐で表示されなかった設問は空欄になる。

アップロード方式は、ファイルアップロードに加えて**Googleスプレッドシートの共有URLを直接参照**する方式にも対応する（`core/ingest.py`、公開または限定公開のシートをCSVエクスポートURL経由で取得）。

**取り込み順序（実装済み、2026-08-25）**: 設問定義表（アンケートフォームHTML/PDFから作成）が無い状態ではRAWデータをアップロードできない（`ui/sidebar.py`の`_render_raw_upload`が`question_definition`の有無を見て、無ければファイルアップローダー自体を表示せず案内メッセージだけを出す）。設問定義表の作成自体はRAWデータの回答パターンに一切頼らない設計（5.1節）だが、必須未回答候補（5.2.2節）の判定に設問定義の「必」マークが要るため、「設問定義→RAW」の順を固定した方が確実というユーザーとの合意事項。

#### 5.2.2 RAWデータ確認
RAWファイルを取り込んだら、集計に入る前に以下を行う。第1タブ「設問定義・RAWデータ確認」の後半（設問定義表の下）にまとめて置く——当初は別タブ「データ確認・クリーニング」だったが、設問定義とRAWデータの整合性を同じ画面で確認したいというユーザーの要望で2026-08-21に統合した。プレビュー（先頭数行の生表示）は、下記の整合性チェックが実質的な代わりになるため廃止した。
- **形式確認**: ファイル名・文字コード・設問数・回答数の表示、および設問定義表とRAWデータの整合性チェック（5.2.3節）
- **TEST回答削除**: フォームのテスト投稿は正式公開前の動作確認として先頭付近の回答に集中する傾向があるため、先頭n件（既定50件、調整可）の全カラムをそのまま人が目視確認し、不要な行を選択して除外する（自動削除はしない。キーワード一致による自動検出は実データ検証で実用に耐えないと判明したため採用していない——6節）
- **文字化けセル候補**: 置換文字を含むセルを検出して一覧表示し、除外するか人が選ぶ
- **空欄提出候補（実装済み、2026-08-25）**: 実質的に何も回答していない行（タイムスタンプ等フォームが自動で埋める列以外に一切値が無い行）を検出して一覧表示し、除外するか人が選ぶ（`core.cleaning.find_blank_response_candidates`、既定チェック済み＝除外）。実データで、フォームを開いてすぐ提出したと思われる空欄回答が複数件見つかり、分岐の無いあらゆる集計表に同じ「未回答」件数として現れて原因調査に時間がかかった実例がある。特定の列名（タイムスタンプ等）を決め打ちで無視するのではなく、「値が入っている列の数が1件以下（既定）」という基準で汎用的に検出する——フォームによって自動入力される列の名前・有無が異なり得るため。**既に除外を確定した行はチェックボックス・確定ボタン付きの候補としては出さず「除外済みの行」として単純に列挙する**（2026-08-27、設計変更——`find_blank_response_candidates`は毎回RAWデータから同じ候補を検出し直すため、除外確定後も同じチェックボックス・ボタンが残り続け紛らわしいという指摘を受けて対応。候補を`excluded_row_ids`に既にあるか無いかで分け、無い方だけチェックボックスを出す。「確定する」ボタン押下時は既に除外済みの行を無条件で維持しつつ、新たにチェックされた行を追加する）。
- **必須未回答候補（実装済み、2026-08-25）**: 設問定義表の「必」マーク（5.1.1節）を使い、必須設問に回答していない行を検出して一覧表示し、除外するか人が選ぶ（`core.cleaning.find_missing_required_candidates`、既定チェック済み＝除外）。空欄提出候補（行全体がほぼ空欄）とは別の観点——他の設問には答えているのに特定の必須設問だけ抜けているケースも拾える。設問定義表が無ければ判定できないため、RAWアップロード自体を設問定義表の作成後に限定した（上記の取り込み順序）。

#### 5.2.3 設問定義表とRAWデータの整合性チェック
設問定義表の設問文とRAW列のヘッダーを突き合わせ、対応が取れない項目（原稿と実装のズレ、実装時の言い回し変更等）を箇条書きで一覧表示する（実装済み、`core/question_definition.py`の`match_to_raw_columns`/`build_consistency_issues`、完全一致優先＋あいまい一致）。将来的には、問題なければ「プロジェクト作成」ボタンで第2タブ（集計指定インターフェイス）を作成する流れにする。

**フォームに存在しないがRAWに必ず入る「タイムスタンプ」列（実装済み、2026-08-22、同日中に方式変更あり）**: Googleフォームのエクスポートには「タイムスタンプ」列が必ず自動追加されるが、フォーム自体の設問ではないためHTML/PDFからの設問定義には出てこない（対応が取れない列として毎回警告される）。**当初はRAWアップロード時に自動登録していたが、実運用で「HTML読み込み直後にRAWを先にアップロードすると、その時点で空だった設問定義表に自動でタイムスタンプ1件だけが入ってしまい、以降HTMLからの初回作成フロー自体が動かなくなる」という実害が発生したため撤回した**（`ui/tab_question_definition.py`の`_render_question_definition`は設問定義表が空の間だけ`_render_import`＝HTML/PDF解析フローを呼ぶ設計のため、RAW側からの自動追加がこの前提を壊してしまっていた）。現在は「➕ 設問を手動で追加」フォーム（ユーザーが明示的に操作するタイミングでのみ実行される、5.1.4節）の既定値を「タイムスタンプ」（形式FA・設問文/短縮設問文とも「タイムスタンプ」）にしておき、送信ボタンを押すだけで追加できるようにする方式に一本化した。この既定値のまま送信すると、固定ID「time」が割り当てられる（`core.question_definition.add_manual_entry`の新しい`entry_id`引数——通常はQ1/Q2/…の通し番号を振り直すが、Qn形式でない既存のIDはこの振り直し対象から除外し、何度add_manual_entryが呼ばれても位置・IDとも保持される。他のフォーム再構築・設問追加でQ番号がずれても、タイムスタンプだけは常に同じID「time」で参照できる）。既に「time」IDの設問がある場合は二重追加せず警告する。

**同一文言の設問が複数ある場合の対応**: 「その他」を選択した方への「具体的にお書きください」という定型の追従質問は、フォーム内に複数回登場することが多く、文言だけでは区別できない（実データで実際に発生）。RAW列見出し・設問定義とも元のフォームの並び順を保持しているため、**あいまい一致の際に既に対応付いた設問を候補から除外しながら列の並び順に処理する**ことで、同一文言でも出現順に正しく1対1で対応付く（別途の隣接判定ロジックは不要だった）。また、フォームに存在しない列（Googleフォームが自動追加する「タイムスタンプ」列など）は5.1.4節の手動追加機能で設問定義側に追加できる。

**値レベルの検証（実装済み、2026-08-22）**: SA/MA設問について、設問定義表の選択肢一覧（フォーム設計上の正規の選択肢）とRAWデータの実際の値を照合し、**選択肢一覧のどれにも一致しない値**を検出する（`core.aggregate.find_unmatched_values`、`core.question_definition.find_unmatched_value_entries`が設問定義表全体に対して実行しレビュー用の構造化データを返す）。MAはカンマ区切りの要素ごとに照合する。照合はNFKC正規化した上で行うため、全角/半角の表記ゆれ（後述）は誤検出しない——それでも一致しない値は、選択肢の言い回しが実質的に異なるか、Googleフォーム標準「その他」インライン自由記述（5.4.1節）である可能性が高い。この検出結果は「その他自動集計の確認」セクション（`ui/tab_question_definition.py`の`_render_other_bucket_review`）で、設問ごとに自動その他バケット化のオンオフを人が確定する材料として使う（5.4.1節）——`build_consistency_issues`自体は列そのものの過不足（対応する列/定義が無い）だけを扱う、より単純な関数に整理した。

**実データで判明した重大な不具合とその修正（2026-08-22）**: ユーザー提供の実プロジェクトで集計結果の一部が不正確になっていた実例を調査した結果、設問定義表の選択肢テキスト（PDFからLLMが抽出したもの、半角括弧「(...)」）と実際のRAWデータのセル値（Googleフォームのエクスポート、全角括弧「（...）」）が食い違い、完全一致による照合では該当選択肢の回答者（実例では対象者の8割超）が集計から丸ごと漏れていた。`core/aggregate.py`の全ての値照合をNFKC正規化した上で行うよう修正——選択肢テキストとRAWセル値の比較は今後も必ずこの正規化を経ること（`_normalize_value`）。あわせて、フォームPDFの「◯◯ 質問Nにスキップします」という選択肢直後の注記をLLMが選択肢テキスト自体に含めてしまい、実際のGoogleフォームエクスポート値（注記なし）と一致しなくなる別の不具合も発見・修正した（`core/form_pdf.py`のプロンプトを改訂——ただし既存の設問定義表は再抽出しないと直らないため、影響のある既存プロジェクトは「🆕 PDFから作り直す」での再作成を推奨）。

**PDFフォント文字化けの再発とNFKC非対応ケースへの対応（2026-08-22、同日追加調査）**: 上記の修正を別の実RAWデータ（CSV）で検証した際、「校長」「青森市」「西津軽郡」「住民」等の常用漢字がNFKC正規化でも解決しない康熙部首ブロックの文字に化けているケース（6節で既知としていた「青は正規化対象外」問題が実際に踏まれた実例）、および「戸」の旧字体「戶」への文字化けが新たに発覚し、値照合の誤検出（6設問分）を引き起こしていた。新設した`core/text_normalize.py`（`fix_known_font_glitches`/`normalize_for_comparison`）に実データで確認済みの文字化けパターンを追加し、`core/form_pdf.py`の`extract_pdf_text`（抽出時点で補正、以後のPDF取込に効く）と`core/aggregate.py`の`_normalize_value`（値照合のたびに補正、既存の設問定義表でも効く）の両方に適用した。全ての康熙部首を網羅的に列挙するのはリスクがあるため、実データで確認できたものだけを確実に直す方針（新しいPDFで別の文字化けが見つかり次第追加）。`core/question_definition.py`の`_normalize_question_text`（列見出し照合）にも同じ補正を適用済み。

**元のRAWデータ列は書き換えない**（TEST回答除外と同じく、このアプリは元データを常に保持し、除外・分類は別レイヤーで管理する方針——6節参照）。検出した「設問定義に無い自由記述」は、回答者との対応（行）を保ったままRAWデータの最右に設問ごとの新しい列として追加する（例:「{短縮設問文}_その他自由記述」）。集計時にこの値を「その他」カテゴリとして扱うかどうかは、集計の計算過程で判断する（元データ・追加列のどちらにも影響しない）。**実装済み（2026-08-22）**: `core/other_text.py`の`build_other_text_columns(df, entries, columns)`がSA/MA設問ごとに該当値をdf行順のSeriesとして抽出し、第1タブ「RAWデータ確認」の形式確認セクションに追加した「➕ その他自由記述をRAWデータに追加する」ボタン（`ui/tab_question_definition.py`の`_render_other_text_extraction`）から`st.session_state['raw_columns']`/`raw_rows`に反映する。既に追加済みの列は再度押しても重複追加しない。**あわせて設問定義表にも自動でFA（自由記述）行を追加する**（`core.question_definition.add_manual_entry`、末尾に追加）——追加した列名をそのまま設問文に使うため`match_to_raw_columns`で自動対応付けされ、「対応する設問定義が見つかりません」という整合性チェックの誤警告を防ぐ（実データで実際に発生し、ユーザー報告を受けて追加、2026-08-22）。

### 5.3 集計指定インターフェイス・集計指定表（第2タブ「集計指定」）

#### 5.3.1 集計指定インターフェイス（チェックボックス方眼）
設問×設問のN×Nチェックボックス方眼。行・列とも短縮設問文（未確定なら原文）を見出しに使う。左端の短縮設問文列は横スクロールしても固定表示する（`st.column_config.TextColumn(..., pinned=True)`）。設問名の直後には設問形式のサフィックス（SA=`|S`・MA=`|M`・FA=`|F`）を表示する（実装済み、2026-08-23、`core.cross_plan.format_suffix`/`question_display_label`）——この方眼はSA/MA設問のみが対象のため実際には`|S`/`|M`のみ現れる。同じサフィックス表示はトリプルクロス指定表のプルダウン（5.3.2節）・データ抽出タブのプルダウン（5.6節）にも適用する。**表示専用**であり、`question_label`（プロジェクトファイルへの保存・照合キーとして使う素のラベル）自体にはサフィックスを付けない——付けると既存の保存済みプロジェクトファイル（トリプルクロス指定表の選択値等）との互換性が壊れるため、`st.selectbox`の`format_func`で表示だけ差し替える設計にした。
- **GT行**（Grand Total行）: このR行のうち特定の列にチェックを入れると、その設問の単純集計（クロスなし）が集計指定表に追加される
- **列一括行・行一括列**: 対象の行/列のチェックボックスを一括でON/OFFする操作補助（データそのものではない）。実装上は表の外に別ボタンとして配置している（`st.data_editor`のセル単位の連動チェックは実装が複雑になるためユーザーと合意した方式、2026-08-21）。
- 通常の行×列セルにチェックを入れると、「属性設問（行側）×対象設問（列側）」のクロス集計が集計指定表に追加される。行×列と列×行は別のテーブルとして扱う（属性側の母数で%を取るか対象側の母数で取るかが異なるため）
- 自分自身との交差（対角セル）は集計対象から除外する

#### 5.3.2 集計指定表
- **【集計表形式】**: 「％実数別表」（％表と実数表を別々に作る）／「％＆実数表」（％と実数を上下に配置）をラジオボタン等で選択。全体設定。既定は「％実数別表」（ユーザーとの合意事項、2026-08-22）。
- **【トリプルクロス指定表】**: 属性設問（大・表最左）×属性設問（中・表左）×対象設問（表頭）の3設問クロスを、短縮設問文のプルダウンから選択する形で最大3セットまで指定できる（2026-08-21、当初は直接入力だったがユーザー要望でプルダウン選択に変更）。グラフ作成機能はない。3項目のうち一部だけ選択されている場合は、確定操作時にアラートを出す。指定・検証（`validate_triple_cross`）・実際の3way集計計算・表示（第3タブ）とも実装済み（`core.aggregate.triple_cross_tabulation`/`core.cross_execute.run_triple_cross`、2026-08-22）。
- **【クロス集計指定表】**: 集計指定インターフェイスのチェックから自動生成される一覧（属性設問・対象設問の組）。行ごとにグラフ種別（縦棒/横棒/円/帯/折れ線、指定なしも可）と、**AIコメントのオンオフ**（チェックボックス列、既定はON）を選ぶ。グラフ指定がなければ集計表のみ作成しグラフは作らない。AIコメントをOFFにした行は、実行時にAIコメントを生成しない（ユーザーとの合意事項、2026-08-22：「指定段階でAI分析のする・しないを選択できるように」）。GT行にはAIコメントは作成されないため、チェックの有無は影響しない。「AIコメントを全選択」「AIコメントを全解除」ボタン（表の外、行一括・列一括ボタンと同じ設計思想）で一括ON/OFFできる（実装済み、2026-08-22）——`st.data_editor`はウィジェットkeyが変わらない限り新しいdataframeの内容より内部状態を優先するため、行一括・列一括の`cross_grid_version`と同じ仕組みで別カウンタ（`cross_table_ai_version`）をキーに含めて強制的に作り直す。
- 内容に問題がなければ「集計計画表を確定」→ 表を保護。「この表を保護する/保護を解除する」で再編集も可能。

#### 5.3.3 一覧型クロス集計指定表（実装済み、2026-08-23。属性/対象の指定方法を同日中に修正。画面表示・AIコメントを同日中に追加）
指定表自体は**個々の属性×対象のペアを指定するのではなく、属性設問のセット（表左側、全ての対象設問に共通）と対象設問のリスト（表頭、1つにつき1表）を別々に指定する**——例えば属性設問を3つ指定すれば、対象設問がいくつあっても表左側は常にこの3設問のまま、対象設問ごとに表が作られる（当初はトリプルクロス指定表を模して属性N／対象Nのペアを5セット指定する設計で実装したが、ユーザーの実際の使い方（1つの属性設問セットを複数の対象設問で使い回す）に合わないと指摘を受け同日中に修正——`ui/tab_crosstab_spec.py`の`_render_list_cross`は属性設問用の5枠（`st.session_state['list_cross_attrs']`）・対象設問用の5枠（`list_cross_targets`）を2カラムに並べて表示する）。加えて、列（対象設問の選択肢）の並べ替えを「全体の出現率が高い方から降順」／「対象設問のデフォルト選択肢順」から選ぶラジオボタン（`list_cross_sort_order`、既定は降順——ユーザー提示の見本Excelがこのモードを使っていたため）。

**画面表示（実装済み、2026-08-23）**: 当初はExcel出力専用の設計だったが、ユーザーの要望で第3タブ「集計表」にも表示するようにした（5.5節）。「▶ 集計表とグラフを作成する」ボタンで他のクロス集計・トリプルクロスと同時に実行・表示する（`core.cross_execute.run_list_cross`の結果を`st.session_state['list_cross_results']`に保存）——グラフ・GT行の対象外である点はトリプルクロスと同じ。表の列順（並べ替え）は`list_cross_sort_order`の設定に従う。

集計ロジックは`core.cross_execute.run_list_cross(df, entries, columns, list_cross_attrs, list_cross_targets)`。既存の`run_cross_plan`と違い、対象設問ごとにグループ化し、既存の「クロス％実数別表」のように属性×対象の組ごとにブロックを分けるのではなく、**指定された属性設問のセット全体を対象設問1つの表にまとめる**——対象設問のリストを外側ループ、属性設問のセットを内側ループとして、対象設問1つにつき指定された全属性設問との組を計算する。「全体」行（属性設問を横断した対象設問全体の分布）は対象設問につき1つだけ持たせ（`cross_tabulation`の「全体」行はどの属性と組んでも同じ値になるため、属性設問リストの最初の1件で計算した結果を採用する）、その下に各属性設問のカテゴリ別分布を並べる。ユーザー提示の見本Excel（`一覧型クロス集計表見本.xlsx`）をセル値まで実測してこの構成を確認した。

Excel出力（`core/excel_report.py`の`_build_list_cross_sheet`）は「一覧型クロス集計表」という名前の5枚目のシートとして追加する（5.5節）。見本で確認した具体的な仕様:
- 対象設問の選択肢列（％表・実数表とも）の先頭行に「元の選択肢番号」（対象設問の選択肢定義順での1始まり位置）を添える——並べ替えで列順が変わっても元の位置が分かるようにするため。その他自動バケット化（5.4.1節）で追加された合成列はこの番号を持たないため空欄にする。
- **％表・実数表とも見出し行の最終列に「全体」という文字を置く（2026-08-26、設計変更）**: 当初は見本にならい、％表の同じ列にはヘッダーテキストを置かない非対称な仕様だったが、％表だけを見たときに母数の内訳が分かりにくいという指摘を受け、％表にも「全体」列（その行の選択肢％の合計、単一選択なら通常100%、複数選択なら100%を超え得る——`_write_list_cross_total_pct`が実際の値を計算する、固定で100と決め打ちはしない、書式は`0%`で小数を出さない）を追加した。その右にもう1列、母数参考列（`n=2123`のようなテキスト）を続ける。実数表側は「全体」列＝横断合計人数がそのままnを兼ねるため列を追加していない（画面表示`ui/tab_crosstab_result.py`の一覧型クロス集計結果も同じレイアウトに揃えた）。
- 0%/0件のセルは「-」等を書かず空欄のままにする（罫線だけ引く）——見本のほとんどの0値セルが空欄だったことに合わせた（1箇所だけ見本に「-」の手入力らしき例外があったが、大多数のパターンを優先し空欄を採用）。
- 属性設問名（例:「年代」）は各属性グループの最初のカテゴリ行にだけ書き、以降は空欄にする（見本の見た目、セル結合ではなく単純に空欄）。属性グループの直後・％表と実数表の境目には見本どおり空行を挟む。

`core/project.py`の`build_project`/必須キー一覧には**あえて含めていない**——後から追加した項目のため、旧バージョンのプロジェクトファイルを読み込んでも失敗しないよう`list_cross_attrs`/`list_cross_targets`/`list_cross_sort_order`は任意キー扱いにした（`.get(key, デフォルト)`で読む）。

### 5.4 ワンクリック実行（第3タブ「集計表」、実装済み）
「集計表とグラフを作成する」ボタン（第3タブ「集計表」の先頭に配置、集計表・グラフ両タブで共有——ユーザーとの合意事項 2026-08-21）で、確定した集計指定表・トリプルクロス指定表・一覧型クロス集計指定表どおりに単純集計（GT行）・2wayクロス集計（属性設問×対象設問）・3wayクロス集計（トリプルクロス）・一覧型クロス集計（5.3.3節）を一括生成する（`core/cross_execute.py`の`run_cross_plan`/`run_triple_cross`/`run_list_cross`）。母数（％計算の分母）は「その設問列が空欄でない有効回答数」を既定とし、単一選択は構成比（合計100%）、複数選択は回答者数に対する選択率（合計100%を超え得る）として算出する。2way/3wayクロス集計は属性カテゴリごとに回答者を分類し（属性がMAの場合、1人が複数カテゴリに属し得る）、そのカテゴリ内での対象設問の分布を%化する（母数＝そのカテゴリに属する回答者のうち対象設問が空欄でない件数）。設問定義エントリの`question_text`とRAW列見出しは文言がずれ得るため、実行時に必ず`match_to_raw_columns`で対応付けてから参照する（entryの`question_text`をそのまま列名として使わない）。選択肢テキストとRAWセル値の照合もNFKC正規化した上で行う（5.2.3節参照）。表・グラフの見出し・軸ラベルには短縮選択肢（無ければ原文）を使う（ユーザーとの合意事項、2026-08-21）。

#### 5.4.2 母数・回答条件・回答率（2026-08-25、当初の「未回答」機能から設計変更）
母数（base、％の分母）は常に「セルに何らかの回答があった数」——`compute_base_count`は空欄セルを常に除外するため、分岐（スキップロジック）で対象外になった回答者も、聞かれたのに答えなかった回答者も、自動的に母数から漏れる。これは前提条件の有無に関わらず最初から正しい（母数だけを見ている限り、対象者を絞り込んでdfを渡しても渡さなくても同じ値になる）。

**廃止した旧設計、記録として残す**: 以前は「未回答」という擬似カテゴリ行/列をGT・クロス・トリプルクロス・一覧型クロスの全表に自動追加し、母数に含まれない空欄セルの件数を「全体」の直前に表示していた（対象設問側は列、属性設問側は行）。未回答の％だけ他の選択肢と異なる分母（母数＋未回答）になる複雑さの割に実益が薄いという判断（ユーザーとの合意事項）で廃止し、代わりに次の2つに置き換えた。

- **回答条件あり表示**: 対象設問に前提条件（5.4.3節）が設定されている場合、設問定義からそのまま「回答条件あり:「〇〇」が「△△」の場合のみ対象」という説明を表示する（`core.cross_execute._condition_label`）。
- **回答率**: 母数（base）÷ 本来の対象者数（`eligible_total`——前提条件があればその条件を満たす行数、無ければ全回答者数）を算出し、n表示に添える（例: `n=1204（回答率100.0%、対象者数1204）`）。genuine non-response（聞かれたのに空欄のまま）の可視性は、専用の行/列ではなくこの回答率で保つ——回答条件が無い設問でも常に表示する（`eligible_total`は常に全回答者数として正確に計算できるため）。
- **表示箇所**（`ui.tab_crosstab_result._render_stats_caption`／`core.excel_report._write_stats_row`が共通ロジック）: GT・クロス（2way・3way）・一覧型クロス集計の各表・各ブロックの見出し直後に、n・回答率・対象者数のキャプション、続けて（条件があれば）回答条件ありの説明を表示する。トリプルクロスは単一の代表的な母数を持たない（`(属性大,属性中)`ごとにbaseが分かれる）ため、対象者数のみ表示する。画面・Excel（クロス％実数別表・旧クロス％実数別表・クロス％＆実数表・一覧型クロス集計表の全シート）とも同じ内容を表示する。

#### 5.4.0 AIコメント（集計表タブ、実装済み。トリプルクロス・一覧型クロス集計への対応を2026-08-23に追加）
クロス集計表（2way）ごとに、属性カテゴリ間の回答傾向の違いを指摘する短い日本語コメントをAIが自動生成する（`core/commentary.py`、ユーザーとの合意事項 2026-08-21）。実行ボタンを押すたびに、集計指定表でAIコメントをONにしている確定済みクロス集計表分をまとめて自動生成する（表の数だけAPI呼び出しが発生するため、既定モデルは低コストのHaiku）。GT行は対象外——「差異」を語れる比較対象が無いため。

**トリプルクロス・一覧型クロス集計にも対応（2026-08-23、ユーザーとの合意事項）**: `generate_triple_cross_commentary`（属性大×属性中の組み合わせ間の違いを指摘）・`generate_list_cross_commentary`（対象設問1つにつき、指定した全属性設問のカテゴリを横断して違いを指摘——1グループ＝1コメント、属性ごとの個別コメントではない）を追加し、実行のたびに確定済みの全トリプルクロス行・全一覧型クロスグループ分もまとめて自動生成する。トリプルクロスは行ごとのAIコメントON/OFFトグルを持たない（指定表自体にその列を設けていないため）——確定した分は必ず生成する。**一覧型クロス集計は、行ごとではなく全グループ一括のON/OFFチェックボックス「AI分析する」（集計指定タブの一覧型クロス集計指定表、`list_cross_ai_comment`、既定True）を持つ**（2026-08-27追加——対象設問1つにつき1コメントで件数が多くなりがちなため、個々の表ごとの指定ではなく全体で1つのチェックにした。OFFの場合`ui/tab_crosstab_result.py`の`_run`は一覧型クロス集計のAPI呼び出し自体をスキップする）。トリプルクロス・一覧型クロス集計とも、比較材料としてAIに渡す表からは「全体」に相当する行（一覧型クロス集計の「全体」サマリー行）を除く——2wayクロスと同じ理由（5.4.0既存の注記参照）。APIキー未設定時は、GT以外の全ての表（2way・トリプルクロス・一覧型クロス集計）でコメントを省略する。

**コメントのスタイル（2026-08-22改訂）**: 実際の出力例で「同じ指摘を言い換えて2文目に繰り返す」「圧倒的に／劇的にといった誇張表現」が冗長との指摘を受け、1〜2文・最も目立つ差1つに絞る・誇張表現を避け「特に」「やや」等の控えめな表現にする・体言止め/形容詞止めも可（無理に「である」調で終えなくてよい）という具体的なスタイル指示に変更した。比較材料としてAIに渡す表からは「全体」行・列（属性カテゴリ横断の合計）を除く——単なる合計値を1カテゴリのように扱って的外れな比較をすることがあるため。

#### 5.4.1 Googleフォーム標準「その他」の扱い（実装済み、2026-08-22）
一部の設問はGoogleフォーム標準の「その他（インライン自由記述、ラジオボタンに直接テキスト入力欄が付くタイプ）」を使っており、回答者が入力した自由記述がそのまま列の値としてエクスポートされる（「その他（次問にお答えください）」＋別設問での自由記述回収、という設計時の想定パターンとは別物）。設問定義表をフォームPDFから作る方式（5.1節）に転換したことで「選択肢一覧」自体はこの影響を受けなくなり、これを逆手に取って**設問定義表に無い値＝その設問固有の自由記述**として検出できる。

**確定した方針**（ユーザーとの合意事項）: 検出は5.2.3節の整合性チェックの一部として行う。元のRAW列は書き換えず、検出した自由記述を回答者との対応を保ったままRAWデータの最右に設問ごとの新しい列として追加する（`core/other_text.py`、5.2.3節）。ユーザーの要望は「特定できた自由記述と回答者属性が切れないようにする」ことで、具体的な実装方法（列追加の形）は一任されている。

**集計時の自動「その他」バケット化——設問定義表に持たせた一度きりの決定として実装（2026-08-22、同日中に設計変更あり）**: 対象設問（GT行・クロスの対象側・トリプルクロスの対象側）に選択肢一覧に無い値が見つかった場合、黙って集計から除外するのではなく、自動的に「その他」という行/列としてまとめて集計に含める。**当初は集計実行のたびに`core/cross_execute.py`側でRAWデータを再スキャンして判定・警告文を組み立てていたが（`find_unmatched_values`を毎回呼び直す設計）、実際には集計をスキップしていないのに第3タブで「一部の集計をスキップしました」という警告が繰り返し表示され、ユーザーから「常に出るのは非常に鬱陶しいし不完全な印象が残る」との指摘を受けた。** さらに「設問もRAWデータの持ち方も、最初にしっかり確定してプロジェクトデータに持たせる方が健全ではないか」という提案（同一調査のRAWをマージするような継続的な追記ではなく、通常はRAWデータから作り直す運用が前提のため、決定を一度だけ確定してもデータとのズレは実運用上問題にならないと確認済み）を受け、**判定そのものを一度だけ行い、決定を設問定義表（プロジェクトデータ）に持たせる設計に変更した**:
- 各設問定義エントリ（SA/MA）は`other_bucket: bool`フィールドを持つ（既定`True`——今までの自動有効化と同じ挙動。`core.question_definition.build_entries`/`add_manual_entry`が設定）。
- タブ1「RAWデータ確認」に新設した「その他自動集計の確認」セクション（`ui/tab_question_definition.py`の`_render_other_bucket_review`、`core.question_definition.find_unmatched_value_entries`で選択肢一覧に無い値がある設問を検出）で、設問ごとにチェックボックスでこの決定を一度だけレビュー・確定する。属性として使う場合はこの決定に関わらずバケット化されない（現状の要望に含まれないため未実装）。
- `core/cross_execute.py`はRAWデータを再スキャンして**警告文を組み立てる**ことはしない——各設問の`other_bucket`フィールドを読むだけ。これに伴い、集計実行のたびに警告文を作っていた`_unmatched_check`/`OTHER_LABEL`定数は廃止し、`run_cross_plan`/`run_triple_cross`の戻り値は素の`(results, issues)`（`issues`は実際に集計をスキップした問題のみ、対応するRAW列が見つからない等）に戻した。
- RAW列への抽出（上記）とは独立した機能——抽出ボタンを押していなくてもこのバケット化は`other_bucket`の値どおりに効く。
- **常に0件になる「その他」列の抑制（実装済み、2026-08-25）**: `other_bucket=True`（既定値）でも、その設問の実データに選択肢一覧と対応しない値が最初から1件も無ければ、その他バケット列/行は常に0件のまま集計表に残り続け、紛らわしい空列になる実害が見つかった（ユーザー提示の見本Excel「不要なその他.xlsx」で確認）。`other_bucket`は「一度でも未対応の値が見つかった設問」だけがタブ1のレビューUI（`_render_other_bucket_review`）に出てくる仕組みのため、未対応の値が最初から存在しない設問はフラグが既定Trueのまま一切見直されない。これは上記「RAWデータを再スキャンして警告文を組み立てない」方針とは別の話として、`_target_other_label(entry, labels, series, options, is_multi)`が対象設問の実データに`find_unmatched_values`で未対応の値が実在するかどうかだけを見る軽い事前チェックを行い、無ければその他バケット自体を作らない（列/行を静かに省く、警告は一切出さない）ように変更した。GT・2wayクロス・トリプルクロス・一覧型クロスの全経路に適用。

**選択肢に既存の「その他」がある場合の名前衝突対策（実装済み、2026-08-22）**: 設問によっては、選択肢一覧に設計上の正式な選択肢として「その他」が既に含まれる（別途、想定外の自由記述も検出される）ケースが実データで見つかった——このとき列名/行名が「その他」で重複し、`st.dataframe`のArrow変換が`ValueError: Duplicate column names found`で落ちる実害が発生した。`core.aggregate.resolve_other_label(existing_labels, base='その他')`が衝突を検知した場合のみ「その他（自由記述）」（さらに衝突すれば連番）にずらす。`core/cross_execute.py`の各バケット化箇所（GT・2way・3way）はこの関数を経由して実際に使う行/列名を決める。

**Googleフォーム標準「その他」のネイティブ入力形式の根本対応（実装済み、2026-08-23）**: ユーザーが実際のフォームのスクリーンショットから、これまで「その他自動集計の確認」で毎回引っかかっていた原因を特定した。GoogleフォームのUIには、ラジオボタンに自由記述の入力欄が直接付いた「その他:」という標準機能があり、この特徴は2つある: (1) 回答者がここに入力した自由文は、あたかも他の選択肢と同じ値としてRAWセルに記録される（選択肢一覧のどれとも一致しない）。(2) 一方でフォームのHTML/PDFから設問定義表を作る際、この入力欄自体のラベル文言「その他:」がそのまま選択肢テキストとして抽出されてしまう——しかしGoogleフォームは「その他:」という文言そのものを回答として記録することは絶対に無いため、この選択肢は集計表で常に回答数0の見せかけの項目になっていた（バグ）。

対応:
- `core.question_definition.build_entries`が、選択肢テキストが「その他:」（または全角コロン「その他：」）と完全一致する項目を検出した時点で選択肢一覧から取り除き、そのentryに`has_native_other: True`フラグを立てる（HTML方式・PDF方式のどちらから抽出しても、この選択肢は必ずこの文言になる——HTML方式はDOM構造上その他の実テキストを持たない入力欄要素のため、PDF方式は`core/form_pdf.py`のプロンプトで原文のまま残すよう指示しているため、両方式で共通のテキストマッチ1箇所の実装で足りる）。`add_manual_entry`で追加した設問は常に`has_native_other: False`。
- `core.question_definition.find_unmatched_value_entries`は`has_native_other=True`の設問をレビュー対象から除外する——この設問の「その他」自由記述の扱いはもはや曖昧な判断ではなく、下記の自動処理で確定的に処理済みのため、「その他自動集計の確認」セクション（タブ1）に表示しない。
- **確認・処理のタイミングはアップロード順に依存しない（2026-08-23、当初実装からの設計変更）**: 当初はRAWデータを新規アップロードした瞬間だけ判定し、確定前に確認を挟む方式だったが、アンケートフォームデータとRAWデータをほぼ同時にアップロードした実例で、判定時点ではまだ設問定義表ができておらず確認が一度も出ないまま処理されない、という実害が見つかった。そのため確認・処理のタイミングを「RAWアップロードの瞬間」から「タブ1（設問定義・RAWデータ確認）を描画するたび」に変更した——`ui/tab_question_definition.py`の`_pending_native_other_entries(entries, columns, df)`が判定する。RAWデータのアップロード自体（`ui/sidebar.py`の`_render_raw_upload`）は他の項目と同じ即時確定のシンプルな挙動に戻した（保留状態は持たない）。
  - **「処理済みかどうか」は設問定義自体に恒久的に記録する（2026-08-26、設計変更）**: 当初は対応する抽出列（`{設問名}_その他自由記述`）が`raw_columns`に存在するかどうかで「処理済みか」を判定していたが、実施中の調査でRAWデータを再エクスポート・再アップロードするたび（回答が増えるたびに繰り返す通常運用）に`raw_columns`が読み込みファイルの列で丸ごと置き換わり既存の抽出列が消えるため、一度「はい」で処理した設問でも確認ダイアログが再アップロードのたびに再び現れ続ける不具合が実データで見つかった。対応として、entryに`native_other_processed`/`native_other_dismissed`（既定どちらもFalse）を追加し、決定そのものを設問定義（プロジェクトデータ）側に持たせることでRAWの再アップロードに影響されなくした。
  - 「✅ はい、処理する」: `has_native_other`の設問だけを対象に既存の`core.other_text.build_other_text_columns`（5.4.1節冒頭の抽出機構）を実行し、自由記述をRAWデータの最右に新しい列として追加、対応するFA設問を末尾に新設する（`add_manual_entry`）。対象設問には`native_other_processed=True`を立て、以後この設問については確認しない。処理完了後、追加した設問名を「〜という設問として追加した」旨でタブ1に一度だけ報告する（`st.session_state['_native_other_processed_report']`）。`native_other_processed=True`の設問は、`_reapply_processed_native_other`がタブ描画のたびに抽出列の有無を確認し、RAWの再アップロードで列が失われていれば確認なしで自動的に再抽出・再追加する（新たに増えた自由記述回答を拾う場合も同様）。
  - 「❌ いいえ、今は処理しない」: 該当設問に`native_other_dismissed=True`を立て、以後この設問については確認しない——対象設問として集計する際の「その他」への自動集計自体は`other_bucket`（既定True）に従って引き続き正しく行われるため、抽出列の作成を後回しにしても集計の正しさに影響は無い。後から「➕ その他自由記述をRAWデータに追加する」ボタン（既存の手動抽出、5.4.1節冒頭）でいつでも処理できる。
- この機構が捕捉するのは「フォーム構造として`has_native_other`と判明している設問」のみ——それ以外の理由（選択肢テキストの言い回しのずれ等）で選択肢一覧に無い値が見つかる設問は、引き続き「その他自動集計の確認」で人が判断する（従来どおり）。

#### 5.4.3 設問の前提条件（分岐条件、実装済み2026-08-25、同日中に役割を縮小、2026-08-27にレビューUIを最終形に変更）
分岐（スキップロジック）のある調査では、ある設問は前の設問の回答によって対象外になり、RAWセルが空欄のまま残る。母数（5.4.2節）はこの空欄を最初から正しく除外するため、前提条件が実際に必要なのは「回答条件あり」の表示と「本来の対象者数に対する回答率」の2つだけ（母数・％の値自体は前提条件の有無に関わらず変わらない）。

**設計**（ユーザーとの合意事項、設問定義表で1回設定する方式を採用）:
- 各設問定義エントリは`condition_entry_id: str | None`（前提条件となる設問のID）・`condition_values: list[str]`（対象とする回答、複数可）フィールドを持つ（既定`None`/`[]`＝条件なし、`core.question_definition.build_entries`/`add_manual_entry`が設定）。
- タブ1「設問定義表」に新設した「前提条件（分岐条件）の設定」セクション（`ui/tab_question_definition.py`の`_render_condition_review`/`_render_condition_editor`）で設定する。設問定義表の確定状態に関わらず（確定後も）設定できる——このシナリオは実運用で確定・集計を試した後に気づくことが多いと想定されるため（「➕ 設問を手動で追加」と同じ配慮、2026-08-22の設計変更を踏襲）。
  - **選択肢型設問すべてをアコーデオンで並べる方式（2026-08-27、最終形。n_note連動の絞り込みは2段階試して両方撤回）**: 当初、n_note（表の「n変化」列、フォームのセクション説明文言から拾った自由記述のメモ）があるSA/MA設問だけをレビュー対象に絞り込んでいたが、n_noteの自動抽出は精度に本質的な限界があり（見落し・過剰検出とも実データで確認済み——HTML方式のセクション単位の伝播ロジックが分岐セクション内のFA follow-up設問など不要なものまで拾ってしまう一方、条件文言がセクション見出しではなく設問文自体に埋め込まれるケースはそもそも拾えず見落しになる）、この抽出結果を機械的にレビュー対象へ出すと精度の問題がそのまま画面に出た。次に「採用」チェックボックス（設問定義表に列を追加、初期値はn_noteの有無から提案）を試したが、それでも過剰検出・見落しの両方が実データで再発した——n_noteの有無で「賢く」提案しようとする限りこの種の精度問題から逃れられないと判断し、**対象の絞り込み自体をやめた**。最終形: 集計軸になり得る設問（SA/MA、`gridable_questions`）を漏れなくすべて用意する（`ui/tab_question_definition.py`の`_render_condition_review`/`_render_condition_editor`）。**外側は「全部を1つのアコーデオンに」という指摘を受け単一の`st.expander`にまとめ、中身は`st.container(height=500)`でスクロールする平たいリストにした**（Streamlitはexpanderの入れ子を許さないため、当初試した1問=1アコーデオン案は撤回——中の各設問は見出し＋区切り線で区切るだけの構成）。各設問の見出しには必ず設問ID（`Q8: ...`の形）を添える。n_noteは各設問の直下に参考のヒントとして表示するのみで、対象の取捨選択には一切使わない——設定するかどうかは完全に人が「n変化」列や設問文そのものを見て判断する。既に前提条件が設定済みの設問は見出し自体に内容（「✅ Q8: 〇〇——「△△」が「□□」の場合のみ対象」）を表示する。**設問1件を見出し1行＋設定1行の計2行に収める**（2026-08-27、当初は見出し・n変化メモ・設定行で3行になっていた指摘を反映）: 見出し行には未設定の設問なら固定の案内文言「対象回答が未設定の場合は前提条件なし」を続け、n_noteがあれば同じ行の末尾にカッコ書きで添える。この案内文言は見出しと差別化するため、`st.columns([2, 3], gap='xxsmall')`で見出しと右側に分け、右側はレギュラーフォント・0.9em・右寄せ（`unsafe_allow_html=True`の`<div style="text-align:right; font-weight:normal; font-size:0.9em;">`）にする（2026-08-27、追加の見た目調整）。設定行は「条件設問」「対象回答」という短いラベルと、ゲート設問セレクトボックス・対象値マルチセレクトを、外側2列（ペア単位、`gap='large'`）×内側2列（ラベル＋入力欄、`gap='xxsmall'`）の入れ子`st.columns`で配置する——ラベルは自分の入力欄には近づけ、条件設問側と対象回答側の2つの入力欄同士は離す、という指摘を反映（`st.columns`は1回の呼び出し内で全境界に同じgapしか指定できないため、ペア内とペア間で異なるgapにするには入れ子が必要）。ラベル列は`st.markdown`、入力列は`label_visibility='collapsed'`のウィジェット。有効/無効の別チェックボックスは持たない（選択の有無自体が意思表示のため冗長）——既定のゲート設問は直前の設問（分岐は「前の設問」を指すことがほとんどのため）。行間そのものも、アコーデオン内側の`st.container(height=500, gap='xxsmall')`で極力詰めている（既定の`gap='small'`のままだと見出し・設定行・区切り線の間が間延びして見えていた）。
- `core/cross_execute.py`の`_eligible_total(df, target_entry, by_id, entry_to_col)`が、対象設問（GT・クロス対象側・トリプルクロス対象側・一覧型クロス対象側のみ——属性側は対象外、other_bucket等と同じ扱い）に前提条件が設定されていれば、`core.aggregate.series_matches_any`で条件を満たす行数（＝回答率の分母）を数える。`_condition_label`が設問定義からそのまま「回答条件あり」の表示文字列を組み立てる。前提条件のRAW列が見つからない場合は全回答者数を使い、理由をissuesに積む（フェイルソフト）。
- **実際の集計（cross_tabulation等）にはdfをそのまま渡す——前提条件によるdfの絞り込みは行わない**（2026-08-25、同日中の設計変更）。母数は元々空欄セルを除外するだけで正しく計算されるため、絞り込んでも結果は変わらない。以前は`_apply_condition`がdfを絞り込んでから集計関数に渡していたが、それは「未回答」という擬似カテゴリ行/列（5.4.2節、廃止済み）を正しく計算するためだけに必要だった処理で、その機能自体を廃止したため不要になった。
- **連鎖的な分岐は再帰的に解決する必要がない**: 前提条件となる設問自身がさらに前提条件を持っていても、対象外の回答者は前提設問のセルも空欄のままRAWに残るため（Googleフォームのスキップロジックの性質上）、直接の前提設問1段だけを見て完全一致でカウントすれば、上流の分岐も自動的に正しく反映される（headlessテストで3段階の連鎖分岐を検証済み）。

### 5.5 結果表示・出力
- **集計表（第3タブ、実装済み。フォーマットはユーザー提示のExcel見本に準拠、2026-08-22）**:
  - **GT（単純集計）**: 番号列＋選択肢＋実数＋%の表に、末尾に「全体」行を付けて表示する（見出しは「{設問}（GT）」）。母数キャプションは「n=1204（回答率100.0%、対象者数1204）」の形式（実装済み、2026-08-25——5.4.2節の回答率表示に統一。旧「n=4141」単体表示から変更）、前提条件があれば続けて「回答条件あり」の説明も表示する。列見出しは「実数」（旧「度数」）。％値は`f'{value:g}%'`で末尾の無駄な0を省いた文字列にする（例: 11%・14.7%・100%）。
  - **クロス（2way/3way）**: 属性カテゴリ（行）に「全体」（属性カテゴリを横断した合計、行）、対象選択肢（列）に「全体」（母数の参考列）をそれぞれ加える。【集計表形式】が「％＆実数表」なら1つの表の中で属性カテゴリごとに実数行→％行を上下に並べる（`種別`列で区別）。「％実数別表」なら％表・実数表を別々の`st.dataframe`として表示する——最左列の見出しは「属性カテゴリ」ではなく「％表」／「実数表」にする（実装済み、2026-08-23。以前は列見出しが「属性カテゴリ」固定で、表の種類は別行の`st.caption`で示していたが、その情報を列見出し自体に持たせて`st.caption`を廃止した）。実数表の数値には「件」を付けない。トリプルクロスは行が(属性大, 属性中)の2階層になる以外は同じ構造だが、「％実数別表」表示時は属性大列の値を各グループの最初の行だけに表示し、以降は空欄にする（見本のセル結合に近い見た目、実装済み、2026-08-23）。
  - **一覧型クロス集計（2026-08-23追加、5.3.3節）**: トリプルクロスと同様、実行結果を「トリプルクロス指定表の集計結果」の下に「一覧型クロス集計指定表の集計結果」として表示する。対象設問1つ＝1エキスパンダー。表の構成は左端に属性設問名（各属性グループの最初のカテゴリ行だけ表示、以降は空欄）・その隣にカテゴリ名（先頭行は「全体」）・対象設問の選択肢列（`list_cross_sort_order`の並べ順）・「全体」列（％表側は選択肢％の合計、実数表側は母数の生の数値）・％表側のみさらに右にn列（`n=X`のテキスト）を続ける——2026-08-26にExcel出力5.3.3節と揃え、非対称仕様を解消した。「％実数別表」「％＆実数表」の両表示形式に対応（既存のクロス表・トリプルクロス表と同じ`_pin`固定表示を使う）。
  - AIコメントはGT行のみ対象外。is_gt=Falseの2wayクロス（集計指定表でONにした行のみ）・トリプルクロス（全行）・一覧型クロス集計（全グループ、対象設問1つにつき1コメント）、いずれも表の下に表示する（2026-08-23、トリプルクロス・一覧型クロス集計への対応を追加。5.4.0節参照）。
- **グラフ（第4タブ、部分実装）**: GT行（単純集計）は横棒グラフをPNGダウンロード付きで表示（`core/charts.py`の`build_bar_chart`を流用、見出しは「{設問}（GT）」）。**属性×対象のクロス集計のグラフ化、および縦棒/円/帯/折れ線の4種類は未実装**——グラフ種別を指定していても「未実装」の案内のみ表示する。次のステップとして実装予定。
- **Excel出力（実装済み、2026-08-22。一覧型クロス集計表シートを2026-08-23追加）**: 第3タブ「集計表」に「📥 集計表をExcel出力する」ボタンを設置（`core/excel_report.py`、`build_report_workbook(results, list_cross_groups=..., list_cross_sort_order=...)`/`workbook_to_bytes`）。画面上の【集計表形式】設定には関わらず、常に5シート構成でまとめて出力する（最初の4シートはユーザー提示の見本Excel「エクセル出力指示.xlsx」の4シート名・レイアウトに準拠、5枚目は5.3.3節の一覧型クロス集計指定表専用）:
  - **「クロス％実数別表」**: 2wayクロスを％表・実数表に分けて縦に並べる（現行の画面表示と同じ全体行/列を末尾に置く並び）。属性カテゴリのラベルには％表側のみ母数を`(n=xx)`付きで表示。
  - **「旧クロス％実数別表」**: 同じ2wayクロスを、全体行・全体列を先頭に置く旧フォーマットで並べる——現行フォーマットに変更する前の見た目に揃えたい既存レポートテンプレート向け（見本にあった、行がずれた「n=xx」の余分な列は見本側のデータ整理ミスと判断し再現していない）。
  - **「GT」**: 単純集計（GT行）を番号列付きで並べる。
  - **「クロス％＆実数表」**: 属性カテゴリごとに実数行→％行を上下に並べる1つの表（画面の「％＆実数表」表示と同じ構造）。
  - **「一覧型クロス集計表」**（2026-08-23追加、5.3.3節）: 一覧型クロス集計指定表の内容を、対象設問ごとに全属性設問をまとめた表として出力する。上記4シートと異なりExcel出力専用の指定（`list_cross_specs`）から作る——画面の集計表タブの`results`とは無関係な独立した計算（`core.cross_execute.run_list_cross`）。
  - トリプルクロスは（一覧型クロス集計表を除き）対象外（ユーザー指定の4カテゴリに含まれないため）。％値はExcelのパーセント書式（`0.0%`）で数値として埋め込む（見本と同じくフォーマット済み小数、文字列ではない）。AIコメントは各クロスブロックの下に1セルとして埋め込む。複数のGT/クロスがある場合、同じシート内に上から順にブロックを積み重ねる。グラフの埋め込みは7節「今後の計画」で扱う。
  - **見た目（実装済み、2026-08-22、同日追加）**: 「数値配置は正しいがレイアウトデザインが無い」という指摘を受け、見本Excelのセルスタイル（font/fill/border/alignment/number_format/列幅）をopenpyxlで直接実測し再現した——表の格子部分には細罫線、見出し・カテゴリラベルは中央寄せ＋折り返し、フォントは游ゴシック（本文9pt/タイトル太字10pt）、「全体」行・「全体」列には薄い塗り（`CCFFFF`）をクロス状に付ける。属性設問名（表の縦軸見出し）はC列に縦方向マージ＋右寄せ（見本の配置に合わせた）。クロス％＆実数表は実数行・％行のペアごとにD列（カテゴリラベル）を縦マージする（見本のD6:D7等のマージパターンに合わせた）。AIコメントのセルは折り返し（wrap_text）を無効にしている（ユーザーとの合意事項、2026-08-22：折り返すと行が伸びて見づらいため、はみ出し表示のままでよい）。

### 5.6 データ抽出（第5タブ、実装済み、2026-08-23）
最初の機能「設問指定RAWデータ出力」（`ui/tab_data_export.py`・`core/data_export.py`、ユーザーとの合意事項）: 全設問の短縮設問名（無ければ原文）が並ぶプルダウンを縦に12行表示し、出力したい設問を指定する。プルダウンの表示には設問形式のサフィックス（`|S`/`|M`/`|F`、5.3.1節参照）を付ける——SA/MAのみが対象のグリッドと違い、このタブは全設問（FA含む）が選択対象のため`|F`も現れる。行ごとに:
- 選択肢型設問（SA/MA）が指定されている場合のみ、値を「短縮型選択肢名」／「原文選択肢名」のどちらで出力するか選ぶラジオボタンが右側に出る（既定は短縮型）。
- 「短縮型質問文」／「原文設問文」のどちらを列見出しにするか選ぶラジオボタンがさらに右側に出る（形式を問わず表示、既定は短縮型）。

タイトル行の右には「最左列に通し番号をつける」チェックボックス（既定オン）と「📥 RAWデータを出力する」ボタンがある。ボタンを押すと、指定した設問だけのRAWデータをCSVでダウンロードする（`utf-8-sig`、Excelでの文字化けを避けるためBOM付き）。表頭は通し番号列が「ID」、各設問列は行ごとに選んだ短縮/原文の設問文。TEST回答として除外済みの行（`excluded_row_ids`）は出力対象から除く。選択肢の値の置き換え（原文↔短縮）はNFKC正規化した上で照合する（5.2.3節と同じ配慮——全角/半角の表記ゆれで置き換え漏れが起きるのを防ぐ）。対応するRAW列が見つからない設問はスキップする。同じ設問を複数行で指定した場合、列見出しが重複しないよう自動的に連番を付ける（後の指定が先の指定を静かに上書きしてしまうのを防ぐため）。

**2つ目の機能「旧設問定義書を出力」（実装済み、2026-08-25）**: 編集中の設問定義から旧型の設問定義書フォーマット（ユーザー提示のサンプルCSVに準拠）へ変換してCSVをダウンロードする。RAWデータの有無に関わらず、設問定義さえあれば使える（このタブの他の機能と異なりRAWデータ不要のため、RAWデータ有無のチェックより前段に置く）。

列構成（`core.question_definition.build_legacy_definition_rows`/`build_legacy_definition_csv`）: ヘッダー行なし・1行1設問。ID・設問ラベル（短縮設問文があればそれ、無ければ原文設問文）・形式（SA/MA/FA）に続けて、SA/MAのみ選択肢数、その後 (番号, 選択肢ラベル（短縮選択肢があればそれ、無ければ原文選択肢）) を選択肢の数だけ繰り返す。新設問定義表の「必」「n変化」「matrix」列と、設問文・選択肢の原文自体は旧フォーマットに存在しないため使わない（ユーザーとの合意事項）。matrix候補（5.1.2節の連番グループ）であっても1設問1行のまままとめない——旧フォーマット自体にグループ化の概念が無いため。

### 5.7 プロジェクトログ（第6タブ、実装済み、2026-08-22）
LLM API呼び出し（設問定義表の初稿作成、AIコメント一括生成）ごとに、作業日時・内容・モデル・トークン使用量（入力/出力/キャッシュ読込/キャッシュ書込）・為替レート・概算コスト（円）を1件のログとして記録する（`core/usage_log.py`、`llm_client.get_token_usage()`の呼び出し前後スナップショット差分から作る）。AIコメント一括生成は表ごとではなく1回の実行にまとめて1件として記録する（表の数だけ行が増えると読みにくくなるため）。プロジェクトログタブでは最上行に合計（トークン・コストは合計値、為替レートは平均値）を表示し、以下に新しい記録を上から順に一覧表示する。プロジェクトファイルに保存され、セッションをまたいで蓄積される。

**為替レートについて**: 実際の為替APIとは連携していない。プロジェクトログタブの最上部に為替レート入力欄（既定値150円/USD）があり、ユーザーがいつでも変更できる（ユーザーとの合意事項、2026-08-22）——変更は以降に記録するログにのみ適用され、既に記録済みのログはそのとき有効だったレートのまま残る（`core/usage_log.py`の`build_entry`が`llm_client.calc_cost_jpy`の固定レートで計算済みのコストをUSDに逆算し、指定されたレートで再計算する）。市場レートのリアルタイム取得ではない概算値であることは画面にもキャプションで明示する。

## 6. 開発の背景・設計判断メモ
- **PDF方式からHTML方式へ（2026-08-22）**: PDF＋LLM方式は、フォントレンダリング起因の文字化け（康熙部首ブロック等、上記の値照合・スキップロジック注記の項目参照）が繰り返し実害を出し、都度その場しのぎの補正テーブルを追加する状態になっていた。ユーザーから「テキストとしてフォームを分析できればベスト」という提案を受け、複数の書き出し形式（MHTML/HTML/CSVエクスポートアドオン）を実際に手を動かして比較検証した結果、**Googleフォームの編集画面をブラウザで保存したHTML**をDOM構造から機械的に解析する方式（`core/form_html.py`）を採用した——LLMを一切使わずに構造（設問文・形式・選択肢）を確定情報として取得できるため、文字化けもLLMの構造判断ミスも原理的に起こらない。比較対象として検討したが不採用にしたもの: (1) MHTML（同じ編集画面をMIME形式で保存——情報量は同じだが埋め込み画像でファイルが肥大化し、解析するHTML自体はプレーンHTML版と同じ）、(2) 個人制作のApps Script経由CSVエクスポート（`FormApp`公式APIを使うため最も正確だが、未検証アプリとしてのGoogle認証確認が毎回発生し「誰でも使える」状態ではないため不採用——ただし参照用の正解データ作成には活用した）。ユーザーとの合意事項:「①専用の抽出システムを持つ、②不具合があれば調査し修復を試みる、③それでもダメならPDF方式」——この方針は既存のPDF方式を廃止するのではなく最後の手段として残す設計判断で、フォント文字化け対策として既に実装済みの`core/text_normalize.py`のような「実データで見つかった問題をその都度直す」姿勢を新方式にも踏襲する。
- **HTML解析はGoogleの内部クラス名でなく安定した信号に頼る**: `class="WcFG6b"`のような内部CSSクラス名はGoogleのUI実装の詳細でありUI更新で変わり得るため、`role="radio"`/`role="checkbox"`のようなアクセシビリティ属性と、`aria-label="質問"`のような実際のユーザー向け日本語UI文言（Googleが気軽に変えるとは考えにくい）を優先する設計にした。それでも将来のGoogle側UI更新で解析が壊れる可能性はゼロではないため、そうなった場合はPDF方式にフォールバックできる（5.1.2節参照）。
- **フォームPDF駆動に転換した理由**: 当初はRAWデータの回答パターンから設問タイプ（SA/MA/FA）を推定していたが、実データ検証で複数の限界が見つかった（6.2・6.3・6.4節）。特にGoogleフォーム標準の「その他（インライン自由記述）」機能は、回答データだけからは設計上の選択肢と回答者の自由記述を区別できない。アンケートフォームそのもの（PDF）から設問定義を作る方式なら、この問題を構造的に回避できる（ユーザーとの合意事項、2026-08-19）。
- **母数の自動算出**: 条件分岐で設問ごとに有効回答者が変わるため、「その列が空欄でない件数」を母数のデフォルトとし、人が確認・上書きできるようにする（ユーザーとの合意事項）。
- **グラフはExcel埋め込みでなく画面表示優先**: 集計表はExcel、グラフはStreamlit（Plotly）上での表現とPNG出力を優先する。Excelへのグラフ埋め込みは将来の拡張とする（ユーザーとの合意事項）。
- **TEST回答検出はキーワード一致をやめた**: 実データ（教員向けアンケート）で検証したところ、キーワード一致は選択肢文自体に「テスト」を含む場合や、アンケートの話題自体が学校の試験に触れる場合に大量の誤検知を起こし（全列走査で918件、自由記述に絞っても33件——ほとんどが「定期テストの回数が減った」等の正当な回答）、実用に耐えなかった。フォームのテスト投稿は正式公開前の動作確認として先頭付近に集中する経験則の方が実データでは信頼でき、キーワード判定より「先頭n件を人がそのまま見て選ぶ」方式の方が結局速いと判断した（ユーザーとの合意事項）。
- **単一選択の自動判定は146選択肢という極端な例で破綻することが判明**: 実データで「ひとつ選択してください」という設問が146種類の値を持ち、単一選択の自動判定（ユニーク値比率が閾値未満なら単一選択）に頼ると選択肢が実質自由記述化して破綻するケースがあった。原因はGoogleフォーム標準の「その他（インライン自由記述）」——設計上は約31個の選択肢のSAだったが、「その他」を選んだ回答者の自由記述がそのまま同じ列に入るため、実データ上は146種類に見えていた。フォームPDF駆動への転換（6.1節）の直接のきっかけとなった実例。
- **集計表タブとグラフタブを分離した理由**: 当初の集計指定表案（5.3.2節初版）では「集計表とグラフを作成する」ボタンが単一の出力画面を想定していたが、集計表（AIコメント付き）とグラフを別々に見返したいというユーザーの要望で、第3タブ「集計表」・第4タブ「グラフ」に分離した（2026-08-21）。実行ボタンは「集計表」タブの先頭に1つだけ置き、両タブ分の結果を1回の計算でまとめて作る（タブごとに別々の実行ボタンを持つ案もあったが、集計計算が二重に走るのを避けるため不採用——ユーザーとの合意事項）。
- **AIコメントは属性間の差異の指摘、Haikuで自動一括生成**: 集計表タブのAIコメントは「属性間の差異の指摘」を主眼にする（数値の平文要約や報告書下書き文ではなく）。生成タイミングは表ごとの個別ボタンではなく、実行時に確定済み全表分をまとめて自動生成する方式を選択（手間がかからない一方、表の数だけAPI呼び出しが発生する）。この設計を踏まえ、コストを抑えるため既定モデルは低コストのHaikuとした（設問定義表の初稿作成に使うSonnetとは別モデル）——いずれもユーザーとの合意事項（2026-08-21）。
- **値照合はNFKC正規化が必須（実データで判明、2026-08-22）**: 設問定義表の選択肢テキスト（PDF由来）とRAWデータの実セル値の完全一致照合は、全角/半角の表記ゆれ（実例: 半角括弧 vs 全角括弧）で簡単に破綻し、該当選択肢の回答者が集計から丸ごと漏れる。5.2.3節で詳述。この教訓は列見出しの照合（`match_to_raw_columns`が既にあいまい一致対応）だけでなく、**値そのものの照合にも同じ配慮が要る**ことを示した——今後、値を比較するコードを書く際は必ず`core.aggregate._normalize_value`（NFKC）を通すこと。
- **PDF由来の選択肢テキストにスキップロジック注記が混入する不具合**: 「◯◯　質問Nにスキップします」という選択肢直後のPDF注記を、LLMが選択肢本体のテキストと誤認して`options[].text`に含めてしまうケースが実データで見つかった（Googleフォームの実際のエクスポート値には注記が含まれないため、そのままでは値照合が常に失敗する）。`core/form_pdf.py`のプロンプトを改訂し、注記は`n_note`側に寄せ、選択肢テキストからは除外するよう明示した（2026-08-22）。
- **AIコメントは属性間の差異の指摘のみ、GT・トリプルクロスは対象外**: 集計表タブのAIコメントは2wayクロスにのみ適用する（GTは比較対象が無い、トリプルクロスはグラフ同様に仕様上対象外）。集計指定表の行ごとにAIコメントのオンオフを選べるようにした（ユーザーとの合意事項、2026-08-22：コスト・必要性は表によって異なるため）。
- **サイドバーは姉妹アプリ（After Coder）とブランディングを揃える**: 上部の見出し（日本語キャッチコピー・アプリ英語名・「KOTONOHA by Marketing Junction」）はAfter Coderと同じCSSクラス・フォントサイズ・配色を踏襲する（ユーザー指定、2026-08-21）。ログイン機能は現時点で未実装のため、After Coderにある「ログイン中」表示は含めていない（認証を実装する際に追加検討）。「設定」セクション（APIキー→プロジェクト名→分析データの特徴の順）以下にPDF・RAWアップロード、プロジェクトファイルのダウンロード/アップロードを配置する構成もAfter Coderに揃えた。プロジェクト名・概要は常時表示の単一入力欄とし、作成前後で同じフィールドをそのまま編集できるようにした（以前は作成後だけ別の展開パネルで編集する2系統の実装だったが、統一して簡素化した）。アプリ名は「Cross Counter GF」（ブラウザタブ・サイドバーのアプリ英語名とも、2026-08-23）、日本語キャッチコピーの文字色は`#ffe5b4`（同日、ユーザー指定）。
- **アップロードにはAPIキーを先に要求する（実装済み、2026-08-23）**: アンケートフォームデータ（HTML/PDF）・RAWデータファイル・プロジェクトファイルのいずれも、APIキーが未入力のままアップロードしようとすると`st.sidebar.warning`を出し、その場ではアップロード処理を行わない（`_loaded_*_file_id`を更新しないため、キー入力後の次の再実行で同じファイルが自動的に処理される）。HTML方式は構造抽出自体にLLMを使わない設計（5.1.2節）だが、この変更によりHTML方式でもアップロード自体にAPIキー入力が前提になる——ユーザーの明示的な指示によるもの（用途に関わらず作業開始前にAPIキーを用意させる運用上の意図）。
- **プロジェクトファイルのダウンロードを適切なタイミングで促す（実装済み、2026-08-23）**: (1) アンケートフォームデータとRAWデータの両方の取り込みが完了した時点でサイドバーに`st.sidebar.info`、(2) 「設問定義表の確定」が完了した時点で第1タブに`st.info`——どちらも一過性のトースト通知ではなく、条件が満たされている間は常に表示され続ける低刺激な案内（ユーザーとの合意事項）。
- **短縮設問文・短縮選択肢の書きぶりをLLMに具体的に指示する（実装済み、2026-08-23）**: 実データで短縮ラベルの精度低下（バッチ分割による文脈喪失、`core/form_html.py`の同日先行の変更参照）が指摘されたのを機に、HTML方式（`core/form_html.py`の`_SHORT_LABEL_PROMPT`）・PDF方式（`core/form_pdf.py`の`_PROMPT_TEMPLATE`）両方のプロンプトへ具体的なスタイル指示を追加した: 回答者属性に関する設問（性別・年齢・年代・住所・勤務先住所・勤務エリア・職業・業種・経歴・職歴・学歴・所属学校・国公立私立・学年・交通手段等）は単語だけの簡潔な形にする、それ以外も「あなたは」「〜ですか」等の会話的表現を避け体言止めにする（例:「〇〇を知っていますか」→「〇〇の認知」）。
- **表形式の画面表示は最左列も固定表示する（実装済み、2026-08-23）**: 集計指定インターフェイスの短縮設問文列（5.3.1節、既存）に加え、設問定義表・RAWデータ確認・集計指定表・集計表（GT/クロス2way/3wayの全表示形式）・プロジェクトログの各`st.dataframe`/`st.data_editor`にも`st.column_config.Column(..., pinned=True)`で最左列（多くはID・属性カテゴリ・番号など）を固定表示するようにした（ユーザーとの合意事項：先頭行だけでなく最左列も横スクロールで見失わないようにしてほしいとの要望）。

## 7. 今後の計画
- **HTML方式での選択肢ごとの分岐先の厳密な抽出**: HTML方式の調査中に、分岐設問では各選択肢の直後に実際の遷移先（「次のセクションに進む」「セクションNに移動」等）が読み取れることが分かった。現状は（PDF方式と同じ）前後の文脈（セクション見出しの文言）からn_noteを推測する方式のままだが、将来的にはこの選択肢ごとの遷移先情報を使ってより厳密な条件分岐の把握ができる可能性がある。
- **属性×対象のクロス集計のグラフ化・縦棒/円/帯/折れ線の実装**: 現状GT行の横棒のみ対応（5.5節）。複数属性カテゴリ×複数選択肢のグループ化グラフが必要で、`core/charts.py`に新しい描画関数が要る。
- **他ツール製アンケートへの対応**: 基本方針どおり、Googleフォーム特化版の完成後に着手する。選択肢番号形式、複数選択のコード番号カンマ区切り（例: "2,4,5"）、列方向フラグ形式（ワイド形式）などGoogleフォーム標準以外の形式を読み込めるコンバーター機能。
- **他アプリ向け出力コンバーター**: 他の分析アプリで使うためのデータ書き出し機能
- **Excelへのグラフ埋め込み**: 画面表示しているグラフをExcelブックに画像として埋め込む
- **PowerPoint出力**: 報告書制作を見据えた設問1つ＝1スライドのPPT出力
- **コーディング集計との連携**: Word_Counterのコードブック方式（自由記述の分類）との将来的な連携
